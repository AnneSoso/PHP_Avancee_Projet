<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Film List{% endblock %}</title>
        <link rel="stylesheet" href="https://bootswatch.com/5/vapor/bootstrap.min.css">
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{ path('home') }}">Film List</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
                </button>

            <div class="collapse navbar-collapse" id="navbarColor01">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('film') }}">All Movies</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('film_add') }}">Add a Movie</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('film_picture') }}">Add a Movie Poster</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('upload') }}">Import a CSV File</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('film_stats') }}">Voting Chart</a>
                    </li>
                </ul>
            </div>
        </nav>
        <div class="container">
        {% block body %}{% endblock %}
        </div>
        {% block javascripts %}{% endblock %}
    </body>
</html>
